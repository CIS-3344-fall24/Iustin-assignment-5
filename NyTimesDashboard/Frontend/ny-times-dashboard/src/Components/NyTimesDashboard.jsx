import {useState} from "react";

const NyTimesDashboard = () => {

    // Declare and initialize variables for the month and articles using `useState`
    const [month, setMonth] = useState(1);
    const [articles, setArticles] = useState(null);


};